<h1>Patients#index</h1>

<%= link_to "New Patient", new_patient_path%>


<h2><%= @patients.length <= 0 ? "No Patients Available in list" : "" %></h2>

<ul>
  <% @patients.each do |patient| %>
  <li><%= link_to patient.full_name, patient %></li>
  <% end %>
  </ul>
